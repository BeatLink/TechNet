# Allowed Defaults ###################################################################################################################################
#
# Option paths the redundant-defaults lint stays quiet about, for settings that are worth spelling out even though they match the default. Entries are
# matched against the full option path and may use `*` as a wildcard.
#
[
]
