# Software Management
#
# This section handles the state version
#

{
    system = {
        stateVersion = "24.11"; # Sets the base version. Don't change unless reinstalling everything
        autoUpgrade.dates = "22:00";
    };
}
