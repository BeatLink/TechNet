# Valent peer settings ###############################################################################################################################
#
# Everything Valent keeps about the peer this host pairs with: per-plugin preferences, and remote commands that are Cinnamon and X11 and would offer a
# phosh phone four commands that silently fail. Host-scoped rather than 0-common's, because a peer only exists on the host holding that pairing.
#
{ }
