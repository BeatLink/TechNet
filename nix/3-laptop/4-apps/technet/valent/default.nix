# Valent commands ####################################################################################################################################
#
# The remote commands Valent offers to a paired device. Odin's rather than 0-common's, because they are Cinnamon and X11: a phone running phosh has
# neither `cinnamon-screensaver-command` nor `xset`, and would offer a peer four commands that silently fail.
#
{ }
